http_port 3128
mime_table /etc/squid/mime.conf

# Force IPv4 for all outgoing connections (cf Docker envs with no IPv6 routing)
tcp_outgoing_address 0.0.0.0

# Allow only common private ranges used by Docker overlays/bridges
acl localnet src 10.0.0.0/8
acl localnet src 172.16.0.0/12
acl localnet src 192.168.0.0/16

# Port/method safety
acl SSL_ports port 443
acl Safe_ports port 443
acl CONNECT method CONNECT
http_access deny !Safe_ports
http_access deny CONNECT !SSL_ports

# Allow only local containers to the allowlisted domains
http_access allow localnet allowed_domains

# Deny everything else
http_access deny all

# Do not cache API/auth traffic
cache deny all

# Log to files that Docker can surface from the container
cache_log /var/log/squid/cache.log
access_log stdio:/var/log/squid/access.log
